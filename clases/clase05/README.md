# Clase 05 - 2 septiembre

sesión guiada por mónica bate

## Resumen (generado por copilot)

La carpeta contiene ejemplos de Processing centrados en el dibujo 2D y en las transformaciones geométricas.

### Contenido

- `imagenes/borrar.txt`: archivo marcador para conservar la carpeta de imágenes.
- `sketch_260902a/sketch_260902a.pde`: ejemplo inicial de dibujo con un lienzo de 600 x 600 píxeles. Usa fondo, punto, línea, rectángulo y elipse, además de `strokeWeight`, `stroke`, `fill`, `noFill` y `rectMode(CENTER)`.
- `sketch_260902c/sketch_260902c.pde`: repite el ejercicio de primitivas gráficas del sketch `a`, sin las líneas comentadas de `setup()` y `draw()`.
- `sketch_260902d/sketch_260902d.pde`: muestra traslación, rotación y escalado de rectángulos mediante `translate()`, `rotate()`, `scale()` y el uso de `pushMatrix()`/`popMatrix()`.
- `sketch_260902e/sketch_260902e.pde`: combina un rectángulo base con otro rectángulo rotado y reducido, usando una matriz aislada y un cambio de relleno.
- `sketch_260902f/sketch_260902f.pde`: continúa la exploración de traslación, rotación y escalado, comparando rectángulos antes y después de cada transformación.
- `sketch_260902g/sketch_260902g.pde`: crea una figura cerrada personalizada con `beginShape()`, varios `vertex()` y `endShape(CLOSE)`.

En conjunto, los ejercicios introducen las primitivas de dibujo, el sistema de coordenadas y las transformaciones geométricas en Processing. Algunos archivos contienen anotaciones de texto junto a instrucciones de código, por lo que pueden requerir revisión antes de ejecutarse directamente.
